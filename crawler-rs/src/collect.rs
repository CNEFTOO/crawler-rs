pub fn collect_subdomain(host_limit: String) -> Vec<String> {
   let collect: Vec<String> = Vec::new();
    collect
}

pub fn collect_domains(host_limit: String) -> Vec<String> {
    let all_domains = Vec::<String>::new();
    all_domains
}